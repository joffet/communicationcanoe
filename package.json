{
  "name": "communication-canoe",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "db:generate": "pnpm --filter @communication-canoe/database db:generate",
    "db:migrate": "pnpm --filter @communication-canoe/database db:migrate",
    "db:studio": "pnpm --filter @communication-canoe/database db:studio",
    "prepare": "node scripts/install-git-hooks.mjs"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5.9.3"
  },
  "packageManager": "pnpm@11.8.0",
  "engines": {
    "node": ">=22.13"
  }
}
